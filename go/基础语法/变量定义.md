#变量的定义
```
package main
import "fmt"
func main() {
    var a int = 100
    var b int = 200
    var c string  = "my string "
    //定义多个
    var (
        dd  int =  1
    )
    ff:=123  //初始化变量
    fmt.Println(a, b ,c ,dd  ,ff  )
}
```